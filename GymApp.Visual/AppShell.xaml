<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="GymApp.Visual.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:view="clr-namespace:GymApp.Visual.View"
    Title="GymApp.Visual"
    FlyoutBehavior="Flyout">

    <Shell.FlyoutHeader>
        <Grid BackgroundColor="{StaticResource Primary}" 
              HeightRequest="100">
            <Label Text="GymApp Pro"
                   FontAttributes="Bold"
                   VerticalOptions="Center"
                   HorizontalOptions="Center" 
                   TextColor="White"/>
        </Grid>
    </Shell.FlyoutHeader>

    <ShellContent Title="Home"
                  ContentTemplate="{DataTemplate view:MainPage}"
                  Route="MainPage" />

    <FlyoutItem Title="Exercises" Icon="dumbell.png">
        <ShellContent ContentTemplate="{DataTemplate view:ExercisesPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Workouts" Icon="calendar.png">
        <ShellContent ContentTemplate="{DataTemplate view:WorkoutsPage}" />
    </FlyoutItem>
    
</Shell>
